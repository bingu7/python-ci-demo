from setuptools import setup, find_packages

setup(
    name="python-ci-demo",
    version="0.1.0",
    description="A simple calculator for learning CI/CD",
    py_modules=["calculator"],
    python_requires=">=3.10",
)
